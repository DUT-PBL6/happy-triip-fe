import { NgModule } from "@angular/core";
import { CommonModule } from "@angular/common";
import { PendingRouteComponent } from "./pending-route.component";

@NgModule({
  declarations: [PendingRouteComponent],
  imports: [CommonModule],
  exports: [PendingRouteComponent],
})
export class PendingRouteModule {}
